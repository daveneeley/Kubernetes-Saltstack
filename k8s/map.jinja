{% set k8s = salt['grains.filter_by']({
        'armv6l': {
            "cpu_arch_map": 'arm'
        },
        'armv7l': {
            "cpu_arch_map": 'arm'
        },
        'x86_64': {
            "cpu_arch_map": 'amd64'
        }
  }
  , grain="cpuarch"
  , merge=salt['pillar.get']('k8s:lookup'))
%}
